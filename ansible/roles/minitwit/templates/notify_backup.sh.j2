#!/bin/bash
# Transition to BACKUP: passive logging only.
echo "$(date): Transitioned to BACKUP state on $(hostname), Floating IP {{ floating_ip }} should be active on the peer" >> /var/log/keepalived-notify.log
